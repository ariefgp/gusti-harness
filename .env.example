# The one and only required secret.
ANTHROPIC_API_KEY=

# Local infra (defaults match docker-compose.yml; override if needed)
REDIS_URL=redis://localhost:6379
DATABASE_URL=postgresql://postgres:dev@localhost:5432/postgres

# Phoenix telemetry UI (local, no signup)
PHOENIX_PORT=6006
